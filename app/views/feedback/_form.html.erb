<%= simple_form_for(@feedback, remote: request.xhr?, html: { data: { modal: true } }) do |f| %>

<div class="modal-body">
  <%= label_tag f.name %>
  <%= lable_tag f.body %>
</div>

<div class="modal-footer">
  <%= submit_tag 'Submit', :class => 'btn-success'%>
</div>
